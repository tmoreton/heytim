# FroggyBot browser viewer

Disposable browser-only shell for the Amazon Bedrock AgentCore Live View client. It is built and tested independently from the Expo application, then copied into the Expo web export as static assets.

The viewer owns no product data or credentials. A signed Live View URL is accepted once through same-origin in-memory messaging and is never written to storage or a route.
