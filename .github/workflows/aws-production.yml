name: Deploy FroggyBot production release

on:
  workflow_dispatch:
    inputs:
      release_scope:
        description: Upload TestFlight builds in CI or deploy the backend for a signed local upload
        required: true
        default: full
        type: choice
        options:
          - full
          - backend-only

concurrency:
  group: frogbot-production-release
  cancel-in-progress: false

permissions:
  actions: read
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    environment: production
    steps:
      - uses: actions/checkout@v7

      - name: Check release branch and production configuration
        env:
          AWS_DEPLOY_ROLE_ARN: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          AMPLIFY_APP_ID: ${{ vars.AMPLIFY_APP_ID }}
          FROGBOT_AGENTCORE_MEMORY_KMS_KEY_ARN: ${{ vars.FROGBOT_AGENTCORE_MEMORY_KMS_KEY_ARN }}
          FROGBOT_ALLOW_SHARED_PRODUCTION_ACCOUNT: ${{ vars.FROGBOT_ALLOW_SHARED_PRODUCTION_ACCOUNT }}
          FROGBOT_YOUTUBE_SEARCH_DAILY_LIMIT: ${{ vars.FROGBOT_YOUTUBE_SEARCH_DAILY_LIMIT }}
          FROGBOT_MONTHLY_BUDGET_USD: ${{ vars.FROGBOT_MONTHLY_BUDGET_USD }}
          AGENTCORE_CREDENTIAL_FROGBOT_OPENROUTER: ${{ secrets.AGENTCORE_CREDENTIAL_FROGBOT_OPENROUTER }}
          AGENTCORE_CREDENTIAL_FROGBOTXAPI: ${{ secrets.AGENTCORE_CREDENTIAL_FROGBOTXAPI }}
          AGENTCORE_CREDENTIAL_FROGBOTYOUTUBEAPI: ${{ secrets.AGENTCORE_CREDENTIAL_FROGBOTYOUTUBEAPI }}
          GOOGLE_OAUTH_SECRET_ARN: ${{ secrets.FROGBOT_GOOGLE_OAUTH_SECRET_ARN }}
          GITHUB_APP_SECRET_ARN: ${{ secrets.FROGBOT_GITHUB_APP_SECRET_ARN }}
          X_OAUTH_SECRET_ARN: ${{ secrets.FROGBOT_X_OAUTH_SECRET_ARN }}
          SLACK_OAUTH_SECRET_ARN: ${{ secrets.FROGBOT_SLACK_OAUTH_SECRET_ARN }}
          NOTION_OAUTH_SECRET_ARN: ${{ secrets.FROGBOT_NOTION_OAUTH_SECRET_ARN }}
          APNS_APPLICATION_ARN: ${{ vars.FROGBOT_APNS_APPLICATION_ARN }}
          GOOGLE_REVIEW_APPROVED: ${{ vars.FROGBOT_GOOGLE_REVIEW_APPROVED }}
          SLACK_REVIEW_APPROVED: ${{ vars.FROGBOT_SLACK_REVIEW_APPROVED }}
          X_REVIEW_APPROVED: ${{ vars.FROGBOT_X_REVIEW_APPROVED }}
          NOTION_REVIEW_APPROVED: ${{ vars.FROGBOT_NOTION_REVIEW_APPROVED }}
          RELEASE_COMPLIANCE_APPROVED: ${{ vars.FROGBOT_RELEASE_COMPLIANCE_APPROVED }}
          APNS_DEVICE_SMOKE_APPROVED: ${{ vars.FROGBOT_APNS_DEVICE_SMOKE_APPROVED }}
          APPLE_TEAM_ID: ${{ vars.FROGBOT_APPLE_TEAM_ID }}
          APP_STORE_CONNECT_KEY_ID: ${{ vars.FROGBOT_APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ vars.FROGBOT_APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_PRIVATE_KEY: ${{ secrets.FROGBOT_APP_STORE_CONNECT_PRIVATE_KEY }}
          APPLE_DISTRIBUTION_CERTIFICATE_BASE64: ${{ secrets.FROGBOT_APPLE_DISTRIBUTION_CERTIFICATE_BASE64 }}
          APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD: ${{ secrets.FROGBOT_APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD }}
          RELEASE_SCOPE: ${{ inputs.release_scope }}
        run: |
          if [[ "$GITHUB_REF" != refs/heads/main ]]; then
            echo 'Production releases must run from main.' >&2
            exit 1
          fi
          if [[ "$RELEASE_SCOPE" != full && "$RELEASE_SCOPE" != backend-only ]]; then
            echo 'release_scope must be full or backend-only.' >&2
            exit 1
          fi
          required_values=(
            AWS_DEPLOY_ROLE_ARN AMPLIFY_APP_ID
            FROGBOT_AGENTCORE_MEMORY_KMS_KEY_ARN
            FROGBOT_YOUTUBE_SEARCH_DAILY_LIMIT FROGBOT_MONTHLY_BUDGET_USD
            AGENTCORE_CREDENTIAL_FROGBOT_OPENROUTER
            AGENTCORE_CREDENTIAL_FROGBOTXAPI AGENTCORE_CREDENTIAL_FROGBOTYOUTUBEAPI
            GOOGLE_OAUTH_SECRET_ARN GITHUB_APP_SECRET_ARN X_OAUTH_SECRET_ARN
            SLACK_OAUTH_SECRET_ARN NOTION_OAUTH_SECRET_ARN APNS_APPLICATION_ARN
          )
          if [[ "$RELEASE_SCOPE" == full ]]; then
            required_values+=(
              APPLE_TEAM_ID APP_STORE_CONNECT_KEY_ID APP_STORE_CONNECT_ISSUER_ID
              APP_STORE_CONNECT_PRIVATE_KEY APPLE_DISTRIBUTION_CERTIFICATE_BASE64
              APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD
            )
          fi
          missing=()
          for name in "${required_values[@]}"; do
            [[ -n "${!name}" ]] || missing+=("$name")
          done
          if (( ${#missing[@]} > 0 )); then
            printf 'Missing production configuration: %s\n' "${missing[*]}" >&2
            exit 1
          fi
          attestations=(
            GOOGLE_REVIEW_APPROVED SLACK_REVIEW_APPROVED X_REVIEW_APPROVED
            NOTION_REVIEW_APPROVED RELEASE_COMPLIANCE_APPROVED APNS_DEVICE_SMOKE_APPROVED
          )
          for name in "${attestations[@]}"; do
            if [[ "${!name}" != true ]]; then
              echo "$name must be set to true after its release checklist item is complete." >&2
              exit 1
            fi
          done
          if [[ "$RELEASE_SCOPE" == full ]]; then
            if ! printf '%s' "$APP_STORE_CONNECT_PRIVATE_KEY" \
              | openssl pkey -check -noout >/dev/null 2>&1; then
              echo 'FROGBOT_APP_STORE_CONNECT_PRIVATE_KEY is not a valid private key.' >&2
              exit 1
            fi
            certificate_bundle="$RUNNER_TEMP/froggybot-distribution.p12"
            trap 'find "$certificate_bundle" -delete 2>/dev/null || true' EXIT
            if ! printf '%s' "$APPLE_DISTRIBUTION_CERTIFICATE_BASE64" \
              | base64 --decode > "$certificate_bundle"; then
              echo 'FROGBOT_APPLE_DISTRIBUTION_CERTIFICATE_BASE64 is not valid base64.' >&2
              exit 1
            fi
            if ! openssl pkcs12 -in "$certificate_bundle" \
              -passin env:APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD -clcerts -nokeys 2>/dev/null \
              | openssl x509 -checkend 86400 -noout >/dev/null 2>&1; then
              echo 'The Apple Distribution certificate is invalid, expired, or expires within 24 hours.' >&2
              exit 1
            fi
            if ! openssl pkcs12 -in "$certificate_bundle" \
              -passin env:APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD -nocerts -nodes 2>/dev/null \
              | openssl pkey -check -noout >/dev/null 2>&1; then
              echo 'The Apple Distribution certificate bundle does not contain its private key.' >&2
              exit 1
            fi
            if ! openssl pkcs12 -in "$certificate_bundle" \
              -passin env:APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD -clcerts -nokeys 2>/dev/null \
              | openssl x509 -subject -noout 2>/dev/null | grep -q 'Apple Distribution'; then
              echo 'The supplied certificate is not an Apple Distribution certificate.' >&2
              exit 1
            fi
            find "$certificate_bundle" -delete
            trap - EXIT
          fi
          node scripts/check-production-config.mjs

      - name: Resolve production AWS target
        id: target
        run: |
          echo "account=$(jq -r '.[] | select(.name == "production") | .account' agentcore/aws-targets.json)" >> "$GITHUB_OUTPUT"
          echo "region=$(jq -r '.[] | select(.name == "production") | .region' agentcore/aws-targets.json)" >> "$GITHUB_OUTPUT"

      - uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ steps.target.outputs.region }}

      - name: Verify immutable non-root release source and account
        run: |
          ./scripts/assert-release-ready.sh --aws
          actual_account="$(aws sts get-caller-identity --query Account --output text)"
          if [[ "$actual_account" != "${{ steps.target.outputs.account }}" ]]; then
            echo "Deployment role resolved to AWS account $actual_account instead of the configured production account." >&2
            exit 1
          fi

      - uses: actions/setup-node@v7
        with:
          node-version: 22.23.2
          cache: npm
          cache-dependency-path: |
            services/API/package-lock.json
            agentcore/cdk/package-lock.json

      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - uses: astral-sh/setup-uv@v10.1.0
        with:
          enable-cache: true
          cache-dependency-glob: services/runtime/uv.lock

      - name: Install production dependencies
        run: |
          npm ci --prefix services/API
          npm ci --prefix agentcore/cdk
          npm install --global @aws/agentcore@0.29.0

      - name: Verify production services
        run: ./scripts/verify.sh server

      - name: Audit production dependencies
        run: ./scripts/audit-dependencies.sh

      - name: Use repository-managed AgentCore CDK dependencies
        run: agentcore config disableDependencyManagement true

      - name: Deploy AgentCore production target
        env:
          FROGBOT_ENVIRONMENT: production
          FROGBOT_AGENTCORE_MEMORY_KMS_KEY_ARN: ${{ vars.FROGBOT_AGENTCORE_MEMORY_KMS_KEY_ARN }}
          FROGBOT_ALLOW_SHARED_PRODUCTION_ACCOUNT: ${{ vars.FROGBOT_ALLOW_SHARED_PRODUCTION_ACCOUNT }}
          AGENTCORE_CREDENTIAL_FROGBOT_OPENROUTER: ${{ secrets.AGENTCORE_CREDENTIAL_FROGBOT_OPENROUTER }}
          AGENTCORE_CREDENTIAL_FROGBOTXAPI: ${{ secrets.AGENTCORE_CREDENTIAL_FROGBOTXAPI }}
          AGENTCORE_CREDENTIAL_FROGBOTYOUTUBEAPI: ${{ secrets.AGENTCORE_CREDENTIAL_FROGBOTYOUTUBEAPI }}
        run: |
          umask 077
          {
            printf 'AGENTCORE_CREDENTIAL_FROGBOT_OPENROUTER=%s\n' "$AGENTCORE_CREDENTIAL_FROGBOT_OPENROUTER"
            printf 'AGENTCORE_CREDENTIAL_FROGBOTXAPI=%s\n' "$AGENTCORE_CREDENTIAL_FROGBOTXAPI"
            printf 'AGENTCORE_CREDENTIAL_FROGBOTYOUTUBEAPI=%s\n' "$AGENTCORE_CREDENTIAL_FROGBOTYOUTUBEAPI"
          } > agentcore/.env.local
          trap 'rm -f agentcore/.env.local' EXIT
          agentcore deploy --target production --yes

      - name: Resolve deployed AgentCore resources
        run: |
          agentcore status --target production --json > /tmp/frogbot-production-status.json
          target='.deployedState.targets.production.resources'
          runtime_arn="$(jq -r "$target.runtimes.FrogBot.runtimeArn // empty" /tmp/frogbot-production-status.json)"
          memory_arn="$(jq -r "$target.memories.FrogBotMemory.memoryArn // empty" /tmp/frogbot-production-status.json)"
          gateway_arn="$(jq -r "$target.mcp.gateways.FrogBotTools.gatewayArn // empty" /tmp/frogbot-production-status.json)"
          if [[ "$runtime_arn" != arn:aws:bedrock-agentcore:* ]] \
            || [[ "$memory_arn" != arn:aws:bedrock-agentcore:* ]] \
            || [[ "$gateway_arn" != arn:aws:bedrock-agentcore:* ]]; then
            echo 'AgentCore production resources are not ready.' >&2
            exit 1
          fi
          echo "FROGBOT_AGENT_RUNTIME_ARN=$runtime_arn" >> "$GITHUB_ENV"
          echo "FROGBOT_MEMORY_ID=${memory_arn##*/}" >> "$GITHUB_ENV"
          echo "FROGBOT_AGENT_GATEWAY_ARN=$gateway_arn" >> "$GITHUB_ENV"

      - name: Deploy Amplify production backend
        working-directory: services/API
        env:
          FROGBOT_ENVIRONMENT: production
          FROGBOT_AGENTCORE_MEMORY_KMS_KEY_ARN: ${{ vars.FROGBOT_AGENTCORE_MEMORY_KMS_KEY_ARN }}
          FROGBOT_GOOGLE_OAUTH_SECRET_ARN: ${{ secrets.FROGBOT_GOOGLE_OAUTH_SECRET_ARN }}
          FROGBOT_GITHUB_APP_SECRET_ARN: ${{ secrets.FROGBOT_GITHUB_APP_SECRET_ARN }}
          FROGBOT_X_OAUTH_SECRET_ARN: ${{ secrets.FROGBOT_X_OAUTH_SECRET_ARN }}
          FROGBOT_SLACK_OAUTH_SECRET_ARN: ${{ secrets.FROGBOT_SLACK_OAUTH_SECRET_ARN }}
          FROGBOT_NOTION_OAUTH_SECRET_ARN: ${{ secrets.FROGBOT_NOTION_OAUTH_SECRET_ARN }}
          FROGBOT_YOUTUBE_SEARCH_DAILY_LIMIT: ${{ vars.FROGBOT_YOUTUBE_SEARCH_DAILY_LIMIT }}
          FROGBOT_MONTHLY_BUDGET_USD: ${{ vars.FROGBOT_MONTHLY_BUDGET_USD }}
          FROGBOT_APNS_APPLICATION_ARN: ${{ vars.FROGBOT_APNS_APPLICATION_ARN }}
          FROGBOT_APNS_SANDBOX_APPLICATION_ARN: ${{ vars.FROGBOT_APNS_SANDBOX_APPLICATION_ARN }}
        run: npx ampx pipeline-deploy --branch main --app-id "${{ vars.AMPLIFY_APP_ID }}" --outputs-out-dir .

      - name: Generate release client configuration
        working-directory: services/API
        run: npm run outputs:apple

      - name: Ensure production meme template catalog
        working-directory: services/runtime
        run: |
          outputs='../API/amplify_outputs.json'
          config='../../agentcore/agentcore.json'
          bucket="$(jq -r '.custom.filesBucketName // empty' "$outputs")"
          prefix="$(jq -r '.runtimes[] | select(.name == "FrogBot") | .envVars[] | select(.name == "FROGBOT_MEME_TEMPLATE_PREFIX") | .value' "$config")"
          if [[ -z "$bucket" || -z "$prefix" ]]; then
            echo 'Production meme-template storage configuration is missing.' >&2
            exit 1
          fi
          catalog_key="${prefix%/}/catalog.json"
          if aws s3api head-object --bucket "$bucket" --key "$catalog_key" >/dev/null 2>&1; then
            echo "Meme template catalog already exists at s3://$bucket/$catalog_key."
          else
            uv run --frozen python scripts/sync_meme_templates.py \
              --bucket "$bucket" \
              --prefix "$prefix"
          fi

      - name: Resolve production observability outputs
        run: |
          output() { jq -r ".custom.$1 // empty" services/API/amplify_outputs.json; }
          logs_key_arn="$(output logsKeyArn)"
          alarm_topic_arn="$(output alarmTopicArn)"
          feedback_role_arn="$(output nativePushFeedbackRoleArn)"
          if [[ "$logs_key_arn" != arn:aws:kms:* ]] \
            || [[ "$alarm_topic_arn" != arn:aws:sns:* ]] \
            || [[ "$feedback_role_arn" != arn:aws:iam::* ]]; then
            echo 'One or more production observability outputs are missing.' >&2
            exit 1
          fi
          echo "FROGBOT_LOGS_KMS_KEY_ARN=$logs_key_arn" >> "$GITHUB_ENV"
          echo "FROGBOT_ALARM_TOPIC_ARN=$alarm_topic_arn" >> "$GITHUB_ENV"
          echo "FROGBOT_NATIVE_PUSH_FEEDBACK_ROLE_ARN=$feedback_role_arn" >> "$GITHUB_ENV"

      - name: Configure runtime logs, alarms, and push feedback
        env:
          FROGBOT_APNS_APPLICATION_ARN: ${{ vars.FROGBOT_APNS_APPLICATION_ARN }}
          FROGBOT_APNS_SANDBOX_APPLICATION_ARN: ${{ vars.FROGBOT_APNS_SANDBOX_APPLICATION_ARN }}
        run: |
          ./scripts/harden-agentcore-logs.sh
          ./scripts/configure-agentcore-observability.sh
          ./scripts/configure-native-push-feedback.sh

      - name: Verify deployed production resources
        env:
          FROGBOT_APNS_APPLICATION_ARN: ${{ vars.FROGBOT_APNS_APPLICATION_ARN }}
          FROGBOT_APNS_SANDBOX_APPLICATION_ARN: ${{ vars.FROGBOT_APNS_SANDBOX_APPLICATION_ARN }}
        run: ./scripts/verify-production-deployment.sh

      - name: Stage production client configuration
        run: |
          staging="$RUNNER_TEMP/frogbot-production-client-configuration"
          install -d "$staging/services/API" "$staging/apps/iOS/Resources"
          install -m 0600 services/API/amplify_outputs.json "$staging/services/API/amplify_outputs.json"
          install -m 0600 apps/iOS/Resources/amplify_outputs.json \
            "$staging/apps/iOS/Resources/amplify_outputs.json"

      - name: Preserve production client configuration
        uses: actions/upload-artifact@v7
        with:
          name: frogbot-production-client-configuration-${{ github.sha }}
          if-no-files-found: error
          retention-days: 30
          path: ${{ runner.temp }}/frogbot-production-client-configuration

  apple:
    name: Release SwiftUI apps to TestFlight
    needs: deploy
    if: ${{ inputs.release_scope == 'full' }}
    runs-on: [self-hosted, macOS, ARM64, frogbot-apple]
    timeout-minutes: 120
    environment: production
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v7

      - name: Restore production client configuration
        uses: actions/download-artifact@v8
        with:
          name: frogbot-production-client-configuration-${{ github.sha }}
          path: .

      - name: Cache native transcription assets
        uses: actions/cache@v6
        with:
          path: packages/frogbot-transcription/ios/Generated
          key: frogbot-nemotron-apple-v3

      - name: Select Apple build number
        id: apple-build
        run: echo "number=$(date -u +%Y%m%d%H%M%S)" >> "$GITHUB_OUTPUT"

      - name: Verify, sign, and upload iPhone and Mac builds
        env:
          APPLE_TEAM_ID: ${{ vars.FROGBOT_APPLE_TEAM_ID }}
          APP_STORE_CONNECT_KEY_ID: ${{ vars.FROGBOT_APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ vars.FROGBOT_APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_PRIVATE_KEY: ${{ secrets.FROGBOT_APP_STORE_CONNECT_PRIVATE_KEY }}
          APPLE_DISTRIBUTION_CERTIFICATE_BASE64: ${{ secrets.FROGBOT_APPLE_DISTRIBUTION_CERTIFICATE_BASE64 }}
          APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD: ${{ secrets.FROGBOT_APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD }}
          FROGGYBOT_BUILD_NUMBER: ${{ steps.apple-build.outputs.number }}
        run: ./apps/iOS/scripts/testflight-ci.sh
