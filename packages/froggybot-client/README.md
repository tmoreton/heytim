# FroggyBot client

Headless transport and command client for FroggyBot applications. The client accepts its service URL and authentication-token provider so it has no dependency on Expo, React, navigation, or a particular authentication UI.
