# FroggyBot Expo client adapter

Platform integration and controller hooks used by the Expo view layer. Network/domain behavior stays in `@froggybot/client`; native services are injected or consumed through narrow package interfaces.
