# FroggyBot contracts

Framework-neutral application DTOs, command interfaces, and generated HTTP routes shared by the backend client and presentation applications. The backend route contract is authoritative; regenerate the route map from `services/API`.
