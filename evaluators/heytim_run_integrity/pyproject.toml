[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "heytim-run-integrity"
version = "0.1.0"
description = "Privacy-safe structural evaluator for HeyTim sessions"
requires-python = ">=3.10"
dependencies = ["bedrock-agentcore>=1.22.0"]

[tool.hatch.build.targets.wheel]
packages = ["."]
