[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "frogbot-run-integrity"
version = "0.1.0"
description = "Privacy-safe structural evaluator for FroggyBot sessions"
requires-python = ">=3.10"
dependencies = ["bedrock-agentcore>=1.22.0"]

[tool.hatch.build.targets.wheel]
packages = ["."]
