diff --git a/node_modules/expo-speech-recognition/ios/ExpoSpeechRecognitionModule.swift b/node_modules/expo-speech-recognition/ios/ExpoSpeechRecognitionModule.swift
index 948661a..2d58a27 100644
--- a/node_modules/expo-speech-recognition/ios/ExpoSpeechRecognitionModule.swift
+++ b/node_modules/expo-speech-recognition/ios/ExpoSpeechRecognitionModule.swift
@@ -540,6 +540,8 @@ public class ExpoSpeechRecognitionModule: Module {
         sendEvent("error", ["error": "not-allowed", "message": recognitionError.message])
       case .recognizerIsUnavailable:
         sendEvent("error", ["error": "service-not-allowed", "message": recognitionError.message])
+      case .onDeviceRecognitionUnavailable:
+        sendEvent("error", ["error": "language-not-supported", "message": recognitionError.message])
       case .invalidAudioSource:
         sendEvent("error", ["error": "audio-capture", "message": recognitionError.message])
       case .audioInputBusy:
diff --git a/node_modules/expo-speech-recognition/ios/ExpoSpeechRecognizer.swift b/node_modules/expo-speech-recognition/ios/ExpoSpeechRecognizer.swift
index 90cdb21..1f7a2f0 100644
--- a/node_modules/expo-speech-recognition/ios/ExpoSpeechRecognizer.swift
+++ b/node_modules/expo-speech-recognition/ios/ExpoSpeechRecognizer.swift
@@ -9,6 +9,7 @@ enum RecognizerError: Error {
   case notAuthorizedToRecognize
   case notPermittedToRecord
   case recognizerIsUnavailable
+  case onDeviceRecognitionUnavailable
   case invalidAudioSource
   case audioInputBusy
   case audioSessionInterrupted
@@ -21,6 +22,7 @@ enum RecognizerError: Error {
     case .notAuthorizedToRecognize: return "Not authorized to recognize speech"
     case .notPermittedToRecord: return "Not permitted to record audio"
     case .recognizerIsUnavailable: return "Recognizer is unavailable"
+    case .onDeviceRecognitionUnavailable: return "On-device recognition is unavailable for this locale"
     case .invalidAudioSource: return "Invalid audio source"
     case .audioInputBusy: return "The audio input is busy"
     case .audioSessionInterrupted: return "Audio session was interrupted"
@@ -220,7 +222,7 @@ actor ExpoSpeechRecognizer: ObservableObject {
     }
 
     do {
-      let request = Self.prepareRequest(
+      let request = try Self.prepareRequest(
         options: options,
         recognizer: recognizer
       )
@@ -568,16 +570,22 @@ actor ExpoSpeechRecognizer: ObservableObject {
   private static func prepareRequest(
     options: SpeechRecognitionOptions,
     recognizer: SFSpeechRecognizer
-  ) -> SFSpeechRecognitionRequest {
+  ) throws -> SFSpeechRecognitionRequest {
     let request = SFSpeechAudioBufferRecognitionRequest()
 
     // We also force-enable partial results on non-continuous mode,
     // which will allow us to re-schedule timers when text is detected
     // These won't get emitted to the user, however
     request.shouldReportPartialResults = options.interimResults || !options.continuous
 
-    if recognizer.supportsOnDeviceRecognition {
-      request.requiresOnDeviceRecognition = options.requiresOnDeviceRecognition
+    // Never silently fall back to Apple's network recognizer when the caller
+    // explicitly requests private, on-device dictation. If the selected
+    // locale does not have an on-device model, Speech reports an error.
+    if options.requiresOnDeviceRecognition {
+      guard recognizer.supportsOnDeviceRecognition else {
+        throw RecognizerError.onDeviceRecognitionUnavailable
+      }
+      request.requiresOnDeviceRecognition = true
     }
 
     if let taskHint = options.iosTaskHint {
